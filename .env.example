# =============================================================================
# 飞书开放平台 SDK 测试环境变量配置
# =============================================================================
#
# 使用方法：
# 1. 复制此文件为 .env：cp .env.example .env
# 2. 填入你的实际凭证信息
# 3. 确保 .env 文件不被提交到版本控制系统
# 4. 运行测试前加载环境变量：source .env
#
# 或者直接导出环境变量：
# export FEISHU_APP_ID="your_app_id"
# export FEISHU_APP_SECRET="your_app_secret"
# etc...

# =============================================================================
# 飞书应用凭证（必需 - 大部分测试需要）
# =============================================================================

# 飞书应用 ID
# 获取方法：
# 1. 访问 https://open.feishu.cn/
# 2. 登录并进入应用管理
# 3. 选择或创建应用
# 4. 在应用详情页找到 "App ID"
FEISHU_APP_ID=cli_xxxxxxxxxxxxxxxx

# 飞书应用密钥
# 获取方法：
# 1. 在同一应用详情页找到 "App Secret"
# 2. 点击查看或重置
# 3. 复制完整的密钥字符串
FEISHU_APP_SECRET=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# =============================================================================
# 飞书群机器人凭证（可选 - 仅群机器人测试需要）
# =============================================================================

# 群机器人 Webhook URL
# 获取方法：
# 1. 在飞书群聊中添加自定义机器人
# 2. 设置机器人名称和头像
# 3. 启用 "签名验证"（推荐提高安全性）
# 4. 复制生成的 Webhook URL
FEISHU_GROUP_ROBOT_WEBHOOK_URL=https://open.feishu.cn/open-apis/bot/v2/hook/xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# 群机器人签名密钥
# 获取方法：
# 1. 在添加机器人时启用"签名验证"
# 2. 系统会生成签名密钥
# 3. 复制并保存此密钥
FEISHU_GROUP_ROBOT_SIGN_SECRET=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# =============================================================================
# 可选配置（用于调试和自定义）
# =============================================================================

# 日志级别（可选）
# 可选值：DEBUG, INFO, WARNING, ERROR, CRITICAL
# 默认：INFO
# FEISHU_LOG_LEVEL=INFO

# 请求超时时间（可选，单位：秒）
# 默认：30
# FEISHU_REQUEST_TIMEOUT=30

# 重试次数（可选）
# 默认：3
# FEISHU_RETRY_COUNT=3

# API 基础 URL（可选，通常不需要修改）
# 默认：https://open.feishu.cn
# FEISHU_API_BASE_URL=https://open.feishu.cn

# =============================================================================
# 测试特定配置（可选）
# =============================================================================

# 测试模式（可选）
# 设置为 true 时，某些操作会使用测试数据而不实际调用 API
# 默认：false
# FEISHU_TEST_MODE=false

# 跳过需要真实数据的测试（可选）
# 设置为 true 时，只运行不需要真实 API 调用的测试
# 默认：false
# FEISHU_SKIP_LIVE_TESTS=false

# =============================================================================
# 使用示例
# =============================================================================

# 设置完环境变量后，运行测试：
#
# 运行所有测试：
# uv run pytest tests/ -v
#
# 只运行不需要凭证的测试：
# uv run pytest tests/test_utils.py -v
#
# 跳过需要真实数据的测试：
# FEISHU_SKIP_LIVE_TESTS=true uv run pytest tests/ -v
#
# 运行特定测试文件：
# uv run pytest tests/test_driver.py -v
# uv run pytest tests/test_sheets.py -v
# uv run pytest tests/test_group_robot.py -v
